#version 450
// x * sigmoid(x), elementwise.

#include "vae_common.glsl"

layout(local_size_x = 256) in;

void main() {
    uint idx = gl_GlobalInvocationID.x;
    if (idx >= pc.aux0) {
        return;
    }
    float x = act[pc.inOff + idx];
    act[pc.outOff + idx] = x / (1.0 + exp(-x));
}
