"""Kev's decision-model code, vendored unchanged for the classification oracle.

model.py, api.py and checkpoint.py are github.com/jaredpalmer/kev at 9fdf054
(2026-09-25), Apache-2.0 (LICENSE beside them). They are the oracle for
CLASSIFICATION.md, so they are copied rather than edited: a fix belongs in
the dump script that drives them, where it is visible.
"""
