[Unit]
Description=ai
After=network.target

[Service]
Type=simple
WorkingDirectory=###
ExecStart=###/ai -addr 0.0.0.0:11434 -token= -llm
;ExecStart=###/ai -addr 0.0.0.0:11434 -token= -embed -tts -tts-gpu -stt -image -previews -edits
Restart=always
RestartSec=1

[Install]
WantedBy=default.target
